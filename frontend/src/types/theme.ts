/** The three possible theme settings. */
export type Theme = 'light' | 'dark' | 'system';

/** The two resolved states after evaluating the system preference. */
export type ResolvedTheme = 'light' | 'dark';
