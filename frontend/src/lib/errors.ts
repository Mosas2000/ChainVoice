/**
 * @module errors
 *
 * Barrel file re-exporting all error-handling utilities and UI
 * components from a single import path.
 *
 * @example
 * ```ts
 * import { getContractErrorMessage, ErrorAlert } from '@/lib/errors';
 * ```
 */

export { getContractErrorMessage, CONTRACT_ERROR_MESSAGES } from './contractErrors';
