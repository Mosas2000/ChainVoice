/**
 * Custom hook that wraps ThemeContext for convenient use.
 *
 * Re-exported from the hooks directory so consumers can write:
 *   import { useTheme } from '@/hooks/useTheme';
 */
export { useTheme } from '@/contexts/ThemeContext';
